import { protector } from "@/api/api";
import Users from "@/components/user/Users";
import React from "react";

const UserPage = () => {
  return (
    <div>
      <Users />
    </div>
  );
};

export default UserPage;
